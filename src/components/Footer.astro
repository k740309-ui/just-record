---
---

<footer class="footer">
  <div class="container">
    <div class="footer-content">
      <a href="mailto:k740309@gmail.com" class="email-link">k740309@gmail.com</a>
      <span class="copyright">© {new Date().getFullYear()} Kimine. All rights reserved.</span>
    </div>
  </div>
</footer>

<style>
  .footer {
    padding: var(--spacing-lg) 0;
    margin-top: var(--spacing-xxl);
    background-color: var(--color-bg); /* Opaque background to hide grid */
    border-top: 1px dashed var(--color-border);
    position: relative;
    z-index: 10;
  }

  .footer-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: var(--spacing-md);
  }

  .email-link {
    font-size: 0.95rem;
    font-weight: 500;
    color: var(--color-text);
    border-bottom: 1px solid transparent;
  }

  .email-link:hover {
    color: var(--color-accent);
    border-bottom-color: var(--color-accent);
  }

  .copyright {
    font-size: 0.85rem;
    color: var(--color-text-muted);
  }

  @media (max-width: 640px) {
    .footer-content {
      flex-direction: column;
      text-align: center;
    }
  }
</style>
